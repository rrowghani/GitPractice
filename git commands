git init
git add
git commit -m ""
git checkout <id>
git revert <id> (go back to a commit, but create a new one)
git reset --hard <id> (i think revert but do not create a new commit)
git status
git log 
git branch (see the branch you are in)
git checkout -b <name> (from the HEAD) (create a branch)
git branch -D <name> (delete)
git merge <name> (merge locally name with Head)
git remote add origin <url> (add username in the url) (creates a link to the url and saves it in origin)
git remote or git remote -v or git remote set-url ... (see and edit the links)
git push origin <branchname> (push local branch to remote repository)
git pull
git branch --set-upstream-to=origin/master master
